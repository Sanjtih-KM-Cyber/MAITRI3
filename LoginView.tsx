import React from 'react';

const LoginView: React.FC = () => {
    return (
        <div>
            <h1>Login</h1>
            {/* Placeholder content */}
        </div>
    );
};

export default LoginView;
